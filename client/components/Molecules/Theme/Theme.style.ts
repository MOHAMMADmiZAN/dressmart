import {createTheme} from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#D4AF11',
            light: '#F5E8A3',
            dark: '#A37F00',
            contrastText: '#000000'
        }

    },

});
